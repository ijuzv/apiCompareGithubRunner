name: Run web and mobile compare

on:
  workflow_dispatch:

jobs:
  compare:
    uses: ./.github/workflows/_compare-reusable.yml
    with:
      run_web: true
      run_mobile: true
      use_live_gate: false
